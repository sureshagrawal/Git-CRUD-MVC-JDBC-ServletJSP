
mysqldump -u root -p crud > crud.sql


docker-compose down -v  # deletes previous container and volumes
docker-compose up --build
http://localhost:8080/


docker-compose down   # delete previous containers (data persists in volume)
docker-compose up     # starts container again
http://localhost:8080/


docker ps

docker exec -it crud-container bash
ls -l /usr/local/tomcat/webapps/
cat /usr/local/tomcat/logs/catalina.out


docker exec -it mysql-container mysql -u root -p
docker exec -it mysql-container bash
ls -l /var/lib/mysql
mysql -u root -p
    USE crud;
    SHOW TABLES;
    SELECT * FROM student;

env
exit


docker volume ls
docker volume inspect crud_mvc_jdbc_servletjsp_mysql-data

